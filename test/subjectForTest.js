/**
 * Test for subjectFor.
 * Runs with mocha.
 */
'use strict'

const subjectFor = require('../lib/subjectFor')
const { ok, equal } = require('assert')

describe('subject-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
